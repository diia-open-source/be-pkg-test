import driverLicense from './driverLicense.js'
import foreignPassport from './foreignPassport.js'
import internalPassport from './internalPassport.js'
import taxpayerCard from './taxpayerCard.js'

const documents: Record<string, unknown> = {
    driverLicense,
    foreignPassport,
    internalPassport,
    taxpayerCard,
}

export default documents
